import TeacherHome from '../../pages/TeacherHome';

// Using TeacherHome logic but wrapping it in the requested component structure
export default function TeacherDashboard() {
    return <TeacherHome />;
}
