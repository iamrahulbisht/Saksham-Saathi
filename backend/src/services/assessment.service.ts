import { AssessmentRepository } from '../repositories/assessment.repository';

const assessmentRepo = new AssessmentRepository();

export class AssessmentService {
    async startAssessment(studentId: string) {
        // Logic
    }
}
